<nav class="top-bar" data-topbar role="navigation">
  <ul class="title-area">
    <li class="name"><h1><%= link_to 'SmartParticipation', root_path %></h1></li>
    <!-- Remove the class "menu-icon" to get rid of menu icon. Take out "Menu" to just have icon alone -->
    <li class="toggle-topbar menu-icon"><a href="#"><!--<span>Menu</span>--></a></li>
  </ul>
  <section class="top-bar-section">
    <!-- Left Nav Section -->
    <ul class="left">
      <li class=" <%= 'active' if params[:controller] == 'articles' %>">
        <%= link_to 'participate', articles_path %>
      </li>
    </ul>
    <!-- Right Nav Section -->
    <ul class="right">
      <% if false %>
          <!-- if !current user-->
          <li class="has-form">
            <ul class="button-group">
              <li><%= link_to 'register', register_path, class: 'button' %></li>
              <li><%= link_to 'login', login_path, class: 'button' %></li>
            </ul>
          </li>
      <% else if false %>
              <!-- else -->
          <li class="has-form"><%= link_to 'logout', logout_path, class: 'button' %></li>
      <% end %>
          <!-- remove one end here -->
      <% end %>
    </ul>
  </section>
</nav>